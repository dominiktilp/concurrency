# K6 folder contain API tests

## How to run tests
```
./run-test.sh BASE_URL TAG
```
This will run tests against ```BASE_URL``` (needs to end by /) and generate reports into ```./reports``` folder. Reports will be named by TAG.